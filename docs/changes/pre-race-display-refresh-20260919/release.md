# 2026-09-19 发布执行记录

用户已明确授权上线；本记录持续以实际结果更新。当前尚未切换线上应用。

## 固定发布包

- PR209，首候选 `801cf77c945e4f2679f8264d69aff0ed39072401` 的只读预演发现2项来源校验问题；修复后重新冻结最终提交和CI，未把本地中断全套视为通过。
- 无模型迁移；沿用 lowcost / 0078 受保护发布协调器，专属数据库备份和恢复意图，四应用重建，保留 DB、Redis、OneBot、Nginx 容器和持久目录，Nginx 按协调器重载。
- 新配置 `RACE_DATA_SYNC_PRE_RACE_REFRESH_ENABLED=true`；`RACE_DATA_RAW_DAILY_PROVIDER_REGION_REQUESTS=512`（原192）；每日每来源/地区字节上限仍1GiB。此请求上限是所有来源/地区桶共用的配置，不是单个来源私有开关；不新增付费服务或搜索覆盖范围。
- 仍按赛场当地 D−4～D−2 3h、D−1 1h、D0 10min；仅既有核验候选和已支持来源进入刷新。失败也消耗2MiB请求预留，512次恰对应1GiB。
- 无人工业务数据写入：491保留原Sporting Life同场来源，其实时页面已明示3/5/6号NONRUNNER。NYRA生产HTTP403，取消来源绑定变更。772已纳管，验证不争用。历史828/830/924继续排除。
- Racing Post直接HTTP406仍视为来源覆盖缺口，不记作更新成功，也不通过浏览器规避。
- 停用新刷新可关闭flag，经受保护流程重载四应用并验证在途任务退出；不直接切旧镜像或恢复整库。已结束的赛事不人为改回赛前。

## 实时预检

2026-09-18 19:44～19:49 UTC：线上实际镜像仍为 `sha256:097820af0b7761b6ff7c5a6c8416c2b7456b3d4f3f6e4aea901b64e7afaa5763`，镜像标签、文件及有效Django设置均对应 `433de627b9ae4d9c0c756dd518668a115a818bd8`。容器环境变量中的旧版本字符串不作为版本依据。

- 八服务运行，Web/DB/Redis健康；双域名healthz返回200。
- 发布锁、实际active intent、restricted recovery标记均不存在；活动导入锁、未结束导入、历史运行批次及匹配采集进程为0。
- 原始快照目录约3.48MB；磁盘可用约46.9GiB，原512MiB高水位与8GiB最小余量保持。
- 已核有效 network/racecard apply 开启，status/odds/popularity字段均准入。最终启用仍核对discovery及四应用一致性。
- 同一来源两场D0上限288次，三场432次；每桶512含余量。当前未纳管预览同桶最多两场，JRA当前窗口103/104/105跨三天；检查既有同桶流量和目录请求，不以单场估算替代总额。19:48 UTC今日桶最大既有用量TRA美国71次，JRA48次；预览来源桶0～2次。

候选目录 `/opt/umanews-release-801cf77c-prerefresh-20260919/umanewsbot` 已按发布锁准备，旧.env仅在服务器内部复制，持久挂载和非本次配置等值校验通过。资料、备份及私有日志留服务器，不导出到仓库。

## 全量验证进展

首候选完整CI暴露1项旧节拍断言，已按真实Beat时点修正并获原reviewer通过，77项相关回归通过。此轮仅修改测试；应用文件与a5228a00一致。重新固定最终提交与CI后再发布。截至本记录，未合并PR209、未切换服务、未启用线上新刷新、未手工改写业务数据。

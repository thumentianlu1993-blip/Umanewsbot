# 45 条旧失败的本次处置

用户要求仅修复已影响线上者，其他跳过。本轮没有增加应用补丁，PR201保持原固定SHA。

以下原始JSON证据均位于本机`C:/coding/umanews/runtime/release_pr201_20260915/`，按2026年9月15日02:37–02:43 UTC的核查窗口解释。

## 核查证据

- `impact.json`：生产只读数据库聚合、9月10日当前容器启动以来的限定日志，入口页面。
- `impact-followup.json`：沿真实首页/马匹列表链接核验双域名公开详情；任务延时队列只读快照。
- `log-classes.json`：Web的326条traceback均为`DisallowedHost`，未见Internal Server Error；四服务日志未达到120000行截断上限。错误Host的扫描请求不作为功能故障。
- `preflight.json`：六场赛果读取允许，行数11/6/7/10/9/11；当前版本、配置、迁移、资源与服务状态。

## 判定与边界

1. TRA旧回执升级字段超长：代码缺陷已知，但生产只存在5次首次导入成功记录，均在8月31日；最近7天无该来源导入/错误记录，现容器日志也无对应字段/调用错误。本轮未证实线上触发，按指令跳过。未将“未查到”写成“所有历史绝无影响”。
2. 历史runner/临时脚本、fixture文件路径、迁移验收工具：未见对应生产调用错误；脚本缺失仍保留为工具恢复事项。本轮不恢复工具、不执行历史数据任务。
3. 非法合成ID、模拟函数新增参数、年份/身份与不可变约束、地区验收样本不完整：测试数据或接口合同需后续维护；不能由这些用例失败推导当前功能中断。本轮不改测试、不降低断言。
4. 旧网页文案/地区筛选断言：公开文案移除已有`1528595`及`docs/current_state.md`中历史P0复验记录支持，页面隐藏内部元数据是已有设计；当前实际公开链接通过。本轮不恢复旧文案或修改现行导航。
5. 部署挂载/脚本枚举、索引能力断言：静态测试与现行配置不同；此次真实挂载、数据库0078空计划、完整45项发布合同及生产健康构成当前发布证据。仍保留原失败，未宣称全量测试通过。
6. 旧race_live样本的provider_contract_missing：现行新同步worker正常，六场已公开赛果通过；旧race_live_worker保持关闭、隔离队列7543，不为测试启动旧链。

初始按NewsArticle.published_at挑选文章出现404。该字段是来源发布时间，存在该字段不等于站内公开；修正为沿首页真实公开链接后双域名文章均200。初始失败完整保留，未作为站内已发布内容丢失的证据。

## 后续触发条件

发现与上述路径相符的实际生产调用失败、合法公开链接异常或业务记录不一致时，再按真实影响修复。缺乏触发记录的后台路径及全部历史范围仍有证据局限，本轮跳过状态不代表永久关闭问题。

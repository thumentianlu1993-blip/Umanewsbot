# 赛事区方案审核记录

范围：仅方案。主线程负责修订，同一子代理 `race_plan_reviewer` 只读审核／复审；未实施或部署。原主线程发现的三项作为本次审核起点，按用户要求转交固定子代理独立核对。输入集中于 PLAN.md 与关联代码／交接文档，不另拆规格流程。

## 第 1 轮：REVISE

审核 PLAN SHA256：`52f41933d8cd3237ab214b73e9567a4650ddbedd888e6253fc51b1617793421f`。

- F1（high）：JRA RUNNERS 改为候选展示后，后台仍对 PENDING 候选提供通用“应用”，可绕回删除重建正式 runner 的旧入口。需在共享 apply 服务拒绝该来源标记的 RUNNERS／BASIC，后台移除应用动作，BASIC 仅走专用受控服务；验证直接 POST、service、批量入口和 TRA 接管前后零越权写入。
- F2：未纳管调度缺口已解决。限定 source_refs 子树、有界枚举、持久 due、短事务 lease、事务外网络和过期响应拒绝已明确。
- F3：独立停用缺口已解决。默认关闭开关、进程级配置生效和旧在途任务退出作为停用完成条件已明确。

## 第 2 轮：APPROVED

同一 reviewer 批准 PLAN SHA256：`3bfc69404b1228ad1a3b2c42338c87fbc7bb2c7479204a54635eff46e528b323`。

主线程修订共享服务拒绝、后台文案与直接入口回归要求后，reviewer 仅复核 F1 及直接影响路径，确认“未发现阻断或需修正的问题”，F1 已在方案层面解决，F2／F3 保持通过。结论：`VERDICT: APPROVED`。审核通过后未修改 PLAN 正文，只回写本记录与交接状态。

本地文档校验：workflow contract 通过，4 项 workflow contract 测试通过，计划链接、用户确认的三档频率及 git diff --check 通过。未运行应用测试，不将文档检查或方案批准写成实现验收。

实施前仍需验证：TRA 详情接口正式性合同、实际预算、JRA 页面阶段及自然调度效果。方案审核通过不能代替这些验证，也不代表生产交付获准。

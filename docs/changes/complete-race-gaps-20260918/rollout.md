# PR205 精确发布包（已执行）

目标：合并[PR205](https://github.com/thumentianlu1993-blip/Umanewsbot/pull/205)到main，并发布固定应用代码 `7e111914d57083299ce452b6222ab9d38476e420`；后续只允许同应用树的验收文档提交。已于05:20 UTC发布为7e111914；此前数据补齐与本次代码发布分别验收，见[发布记录](release.md)。

## 范围

- 新增人工核验参考卡与NAR官方卡的赛前只读展示，标签区分来源；读取本轮已经入库的9场77条候选。TRA正式完整卡接管后整份隐藏。
- 服务层拒绝赛前候选经通用apply写canonical，来源改名但raw标记仍在也拒绝。
- 无模型/数据库迁移；不改配置、3h/1h/10min、预算、来源自动调度或功能开关，不打开旧LIVE。
- 不额外执行生产资料写入。历史129场1209行和赛前9场77条已回填，发布仅读取现有候选；其余历史3、未来10缺口不在此发布中强制变更。
- 标准lowcost/0078流程：独立release目录、固定SHA构建、生产资源锁、活动导入/抓取与恢复状态预检、备份及恢复清单；重建web、worker、beat、race_sync_v2_worker四应用。保留DB/Redis/OneBot，Nginx配置检验后重载。

## 验证与技术门槛

- 70项相关PostgreSQL测试通过，包含公开页退赛状态、时间跨日、完整基线、字段锁/owner/并发接管、事务回滚、服务器私有before路径、NAR原文时区/官方语义和通用apply旁路。独立库进程退出0；早期共享测试库清理冲突不计完整通过。
- 固定子代理来源和代码审核通过，执行包提交绑定问题已修复。全量CI必须在最终应用代码上完成基线/候选失败集合比较后才能部署；仅定向测试或旧SHA的成功不满足此项。
- 发布后核验四进程文件/镜像/settings版本、空迁移计划、DB/队列/服务/锁/备份及release complete；双域名全部9场77条预览、官方与参考标签、3场JRA旧预览、历史129页不回退。
- 77条中若正式TRA卡已经接管，验证应以正式卡无重复为准，不能为了凑数恢复旧候选。随着比赛开跑，原窗口按代码自然终止；不得篡改赛时延长展示。

## 回滚

若新代码验收失败，保留数据与审计、停止进一步交付；按原发布合同对候选展示改动进行前向回退/禁用并复验。不能整库恢复覆盖本轮赛果及其他业务，不能删除候选冒充恢复，也不恢复旧LIVE过期授权。变更范围需要扩展时按根AGENTS执行。

## 确认边界

早前自动审批因精确发布包尚未确认而拒绝合并。用户随后明确回复“确认，可直接上线”，已授权本包合并和发布；PR205已标记就绪。技术门槛仍须通过，实际合并、部署与验收以 release.md 的完成记录为准。

# 赛事线上验收与资料回填

用户于 2026-09-18 明确要求线上验收并补齐全部缺口，覆盖此次清单内的来源采集、精确身份修复、已上线 JRA 受控路径启用和有证据的数据回填；不发送外部通知、不购买新订阅、不修改既有额度、不移除人工锁，不把第三方资料伪称官方。主线程操作，既有只读 reviewer 审核。

## 最新基线

生产 d0bb40fb，四应用运行约9小时，Web healthy。03:08 UTC 只读检查：今年132场过期赛事无已确认结果（包含已有暂定行），未来7天22场缺时间/出马表；清单155场含一个已正常纳管的对照。2025及以前无本次口径缺口。具体字段、owner与源状态见 服务器内冻结清单及本地受限证据；原79场并非全量。

## 顺序

1. 逐域名检查公开入口、实际版本、数据库、队列和锁。按数据 owner 分流，固定目标基线。
2. 日本赛前先启用已经发布且审核过的 JRA 窄路径（D−4、3h/1h/10min、现有预算）。本轮当前符合窗口的中央赛事103/104/105；官方预检104已编号、105名单未编号，两场15:45日本时间。103官方标题与原缩写不一致，先独立核实精确别名后修复，禁止放宽名称匹配。
3. 历史优先复用旧缓存/候选；新增抓取按地区准确日期和URL，不扫全站。不直接用旧通用detail importer覆盖正式表；它缺少owner保护且默认确认。已纳管的828/830等走原data_sync claim/receipt/revision/publication链路；无归属对象才准备独立审核回填。
4. 旧回填入口发现事务内owner/锁检查、字段保留、同着/非完赛行等缺陷。若需要修代码，先写实际失败测试再窄修，原reviewer复审。每批有来源完整性、身份、SHA基线和备份，dry-run后apply、逐场DB与公网验证。
5. 未公布未来资料不可能立即填满；记录实际公布阶段及后续due，不伪造时间/编号/正式赛果。全部清单最终逐项标为已补齐或有具体来源/身份阻塞，不能把改状态当资料回填。

## 首批 JRA 配置操作包

固定代码仍为 d0bb40fb8defe3e9de1029aa255edd2e06e1e0c6，同现有镜像；新独立服务目录、沿用原持久根与TLS。唯一配置差异为 RACE_DATA_SYNC_JRA_PRE_RACE_ENABLED=false→true。使用现有deploy_lowcost/0078 coordinator；新备份/intent，无迁移，Web/worker/Beat/v2 worker按原范围重建，DB/Redis/OneBot保留、Nginx检验重载。新旧目录不覆盖，未完成原intent禁止重试新release。

上线前复查采集/锁；当前JRA 3场正常最坏完整日详情177次以内（含目录），限额192保持。103身份未完成前仅记明确失败、不写赛事。开启后通过原discover_jra_pre_race执行一次受控补跑，四应用实际settings均true。检查104/105候选名单、实际赛时、来源摘要；canonical runner不被JRA写入。随后观察下一自然tick，不能把人工补跑宣称自然验收。

停用沿既有JRA流程：停派发、排空旧任务，以false重建应用并逐进程验收。发布完成后问题前向修复；不恢复整库覆盖其他业务。历史回填包另按实际来源证据逐批冻结，不借配置发布自动确认132场赛果。

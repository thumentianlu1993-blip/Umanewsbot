> 2026-09-18 最终状态：已合并、上线并启用，详见[正式发布验收](release.md)及[最终CI摘要](ci-summary.json)。下文保留实施／预检各时点记录；其中“未授权／未推送／未发布／待验证”不再代表当前状态。

# 发布与恢复说明

当前发布准备状态见[实时预检与发布包](release-preflight.md)。原实现轮只交付代码及默认关闭的展示开关。无schema迁移、无历史回写、无采集频率或队列变更。生产未读取／未操作。交付权限统一遵循根 `AGENTS.md`。

未来发布包绑定最终PR／SHA，先部署关闭开关的代码并验证旧页面。开启 `RACE_INFORMATION_NORMALIZED_DISPLAY_ENABLED=true` 需按实际配置加载方式更新和重启相应应用；部署工具如果同时重建worker，应据实列出影响。不能据本次默认关闭代码推断生产已开启。

启用前使用离线或显式范围只读报告，核对日本、中国香港、英国、法国、美国样本，解释全部 unknown/conflict；新模式不猜 `m` 和裸数字，旧来源未提供明确单位时会待核实。采集任务与发布的锁／版本合同由发布协调者实时核验，本任务未暂停任何采集。

验收入口：`/races/`、赛事详情（包括赛前名单、赛果、冠军和历年冠军）、首页快讯与侧栏、新闻赛事卡片、马匹履历各页、后台候选预览。验证距离／等级无禁用形式、译名一致、未公开赛果仍隐藏、查询无N+1，并抽查旧开关四种组合。新闻正文及原名对照不在结构化字段替换范围内。

恢复：新开关改为 false 并按运行方式重新加载配置，验证模型属性、词库和模板恢复旧路径。没有业务写入，无需逆向数据迁移；当前0078合同禁用普通代码rollback，不能直接恢复旧镜像；发布中断用同候选与原intent续跑，完成后的问题前向修复。现有 `race_event_public_cache.py` 只缓存数量／年份，本次没有新增全页或片段共享缓存，不清空共享Redis。

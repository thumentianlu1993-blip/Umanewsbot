# 2026-09-18 发布准备（尚未发布）

更新：用户已明确授权向公开仓库及目标分支推送、合并并上线。单位兼容修复及35项限定测试、同一reviewer复审APPROVED已完成，等待候选镜像实库只读聚合及PR Linux CI。以下预检问题记录保留发现时事实。

用户已明确要求“可以开始正式发布上线了”。当前代码 HEAD 为 `89c52aca36fa17b99bcaf32cc43e67ff71633737`，应用实现提交 `71a820bd80d3ea68437a55416c54accea73b5004`；远程 main 只读核验仍为 `91410e7aa077a0b3ea963cccfcd64b935f4369d1`。没有推送、PR、合并或生产配置修改。

## 实时预检

2026-09-18 08:32 UTC 只读核验：实际目录 `/opt/umanews-release-7e111914-PR205-20260918/umanewsbot`，四应用仍为镜像 `sha256:8f3110748047e69636f2f1bebbd4eb32ed48018fde1f463c9bb8c297dc7788d3`。八服务运行、无 OOM、重启计数为0，Web/DB/Redis healthy；无部署锁、未完成发布意图、恢复限制标记、活动导入锁、未结束导入、活动历史批次或匹配的采集进程。磁盘可用约48.5 GiB。实际执行前须重查，不以此时间点替代后续运行态。

本机马匹采集现有暂停记录为批次8完成219匹（217候选、2缺口），剩余281，下一匹 `hrs_38905307`，最后完成 `hrs_38884811` / 请求 `r0566`。本轮未恢复或修改采集任务；该检查点引用当前文件而非旧的42/500记忆。

## 单位兼容尚未通过

服务器只读事务内，按五地区各取2026年9月1日起最早的5场已公开赛事，最多25条。仅返回聚合计数，没有导出赛事明细或 source_refs；未调用付费来源或进行业务写入。

| 地区 | 样本 | 距离缺失 | 使用m且没有显式单位元数据 |
| --- | ---: | ---: | ---: |
| 日本 | 5 | 0 | 2 |
| 中国香港 | 5 | 0 | 5 |
| 英国 | 5 | 1 | 0 |
| 法国 | 5 | 0 | 5 |
| 美国 | 5 | 0 | 0 |

25条均未提供新适配器读取的 `source_refs.field_units.distance_text`。当前严格规则会使至少12条距离显示待核实；不能把其余13条视为已完成所有字段验收。现有写入归一化器也未把来源单位合同传入，不能直接把旧推算字段当官方单位证明。启用前需要核对实际来源和字段合同、补齐适配并做局部回归；不按地区或数字大小猜测，不为适配批量改生产记录。

## 发布包与恢复

目标仓库为公开 GitHub `thumentianlu1993-blip/Umanewsbot`，分支 `codex/normalize-race-information`，目标 main；正式站点 `umafans.run` / `www.umafans.run`。待解决单位兼容及远程授权后，绑定最终代码SHA和PR执行：

- 无新数据库迁移、历史数据回写或新采集任务；配置仅新增展示开关。
- 用现有lowcost/0078协调器，独立release目录、固定SHA镜像、发布锁、专属备份及恢复意图；保留持久目录和TLS。
- 重建 web、worker、beat、race_sync_v2_worker 四应用；保留 DB/Redis/OneBot，核验并重载 Nginx。新开关经现有 `env_file: .env` 传入，不缺Compose加载链路。
- 先在候选镜像验证关闭态及强制归一化的只读预览，再在单位兼容、页面及性能验收通过后部署并启用 `RACE_INFORMATION_NORMALIZED_DISPLAY_ENABLED=true`。不得将只部署默认关闭代码称为归一化已上线。
- 本机仅运行变动所需测试；现有31项新增、93项相关PG测试已通过。PR Linux CI才承担全量对照，不重复本机整仓；历史失败保留真实状态。
- 验证两域名健康、赛事列表/详情、赛前名单/赛果权限、首页卡片、新闻关联赛事和马匹履历；核对实际镜像文件/标签/settings、空迁移计划、服务、备份、intent/complete和锁收尾。
- 展示异常优先将新开关恢复false并通过受保护流程重载、复验；0078普通代码rollback继续禁用，不直接切回旧镜像或恢复整库。中断只沿本次原候选及SHA绑定intent续跑，完成后的代码问题前向修复。

## 尚未通过的自动审批

首次远程push被自动审批拒绝：要求用户明确授权向上述公开仓库及分支外发代码和文档，仅“正式发布上线”未被该审批器视为目的地授权。未尝试其他推送或bundle方式绕过。

一次25条生产样本明细导出到本机临时目录的请求也被拒绝，原因是source_refs元数据可能非公开且导出目的地未明确授权。已改为服务器内只读聚合并成功，不再导出明细；若后续确需本地样本，须先取得该数据范围/目的地的明确授权。

当前剩余项：候选镜像单位兼容聚合复核、最终PR/CI/合并、生产部署/启用/页面验收。预检通过不代表已发布。

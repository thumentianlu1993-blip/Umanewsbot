# 香港2026/27新赛季日历与已完赛信息导入

用户明确要求导入香港新赛季，已选择沿用现有赛事区口径：G1/G2/G3及已收录系列；历史828/830/924三场暂缓，排除本轮。主线程执行，沿用同线程固定子代理只读审核。

## 当前事实与范围

官方getSeasonRaces中英文2627清单一致，共35场（32场G1–G3、3场已收录四岁经典系列）。生产运行7e111914，已有本季2026香港杯event2；其余34场未见。9月6日行政长官杯为G3且官方已有6条正式赛果，其余尚未举行。保留实际公历年为year/edition_year，source_refs保存2026/27赛季；官方日期字段为香港零点，不能冒充开赛时间。

## 最小实现

- 冻结中英文官方原始清单、赛场/时间来源和已完赛单场赛果，保存URL/抓取时间/SHA；逐场映射既有RaceSeries，新行政长官杯另建精确系列。
- 使用仅适用于本批官方赛季manifest的一次性脚本；默认dry-run。保留现有香港杯及其路径和所有业务字段，不覆盖存量。创建34场日历；已结束1场在同事务写完整参赛信息和正式赛果、finished/确认时间。
- 不改变年度历史import_race_events的due-only门禁。新脚本是用户本次明确授权的赛季日历导入，范围锁死；不新增通用采集平台、表、调度、配置或部署，不开放旧LIVE。
- 保留G1/G2/G3原等级，4YO使用“其他”规范等级和四岁经典系列原文，不标作国际G1。未来未公布的赛时/名单保留缺失，不生成零点或猜测参赛马。
- 幂等、来源/manifest/脚本SHA绑定、事务级导入锁、已有系列与赛事身份查重、维护门禁；已有记录/归属漂移停止。全批一次事务，故障回滚；生产备份与私有before留服务器，审计保留变更范围。

## 先行测试与验收

先补测试：香港零点换日、公历年/赛季、35场完整性、中英错配/来源漂移拒绝、未来赛时为空、四岁赛不升格、既有赛事不覆盖、幂等重放、重复/身份漂移拒绝、历史场次无完整赛果拒绝、故障事务回滚、真实公开赛后页。

定向隔离PostgreSQL测试和固定子代理审核通过后，固定SHA脚本生产dry-run、备份/锁/再核验、apply；复验所有35场身份和数量、行政长官杯6条官方赛果、香港杯不变，两个域名公开列表/详情及赛后状态。新脚本不要求重建应用；如果需要额外应用能力则另行列明，不扩大本包。

## 已完成的测试与来源核对

- 先行RED：测试模块因尚无导入脚本而失败；随后完成实现和12项隔离PostgreSQL回归，最终全部通过。
- 独立核验双语官方API各35条、所有公历日期/距离/奖金一致。原始API的香港零点仅作为日期。
- PDF第18页确认行政长官杯香港时间13:30、沙田草地A栏1200米；第52–53页确认未来赛场，一月杯为跑马地。
- 双语单场结果完整6匹；保存官方HK马匹链接、原始繁体/英文行和简体展示，不新建马匹身份。
- 维护gate阻断、故障全批回滚、重复/漂移拒绝、源SHA、35场/跨年/4YO、重放幂等、香港杯完整快照不变、实际公开页“已完赛”及6结果、未来页“开赛时间待补”均有测试。
- 生产只读预检：固定应用7e111914；8容器运行，外部导入锁/未结束导入/活跃历史批次/维护gate均0，无采集进程，磁盘剩余约49GiB。此次只读快照hash已写manifest；历史828/830/924另留摘要，不导出其私有载荷。

## 精确生产操作

受审入口为`run_reviewed_hk_season.py`，只执行本批`import_reviewed_hk_season.py`。以seal绑定两脚本、manifest及5个原始源文件；同一共享deployment_lock下检查实际应用版本、服务、恢复intent和采集进程，默认只做dry-run。apply先保存0600私有before及用既有backup_db.sh创建并校验custom格式全库备份，再重验基线、一次事务导入、立即幂等verify。

不存在应用部署、迁移、开关、额度、通知、容器重启或旧LIVE启动。新日历沿用等级优先级（G1=P0；G2及4YO=P1；G3=P2），可在全部赛事筛选查看；不扩大为普通赛日的全部场次。用户已明确授权把这些赛事拉入并补全已结束赛事，本轮数据操作在该范围内。

失败事务自动回滚。若提交后的线上验收发现异常，停止后续写入，保留回执和私有before/backup；只针对回执中本次创建ID评估修复，不直接全库恢复或批量删除其他业务数据。

固定代码代理第二轮指出宿主执行中断释放锁及Healthcheck缺口；主线程先补3项RED故障测试再修复。最终12项全部通过：apply已开始但未确认终态时保留共享锁与异常收据，核实容器内进程/事务/回执终态后方可释放；带Healthcheck容器必须healthy。操作异常不通过“客户端退出”猜测业务已终止。

## 完成状态

本批已导入生产并完成所有计划中的DB、幂等、双域名详情与跨年日历验收，详见[验收清单](acceptance.md)。先前“准备/待review”等段落记录各阶段事实，以本完成记录为准。

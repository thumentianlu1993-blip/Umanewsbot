schema: spec-driven
created: 2026-07-22
profile: feature
phase: implementing
journal:
  - event: plan-reviewed
    date: "2026-07-22"
    session: 1
    mode: full
    rounds: 1
    issues_resolved: 11
    artifacts_modified: [design.md, tasks.md]
    note: "Round 1 resolved 1 P0 + 6 P1 + 4 P2: P0-1 client-selection layer does not exist (RegionAdapter has no client_factory; prepare builds one client per region) — selection moved to select-stage namespace preference for netkeiba plus a composed dispatcher client at _CLIENTS[japan]; P1-1 comparator is adapters' _normalized_text and client strips the parenthesized country suffix before identity.horse_name (aliases carry raw + romanized names); P1-2 year-only birth date cannot pass the payload validator — outcome is fail-closed candidate block, not precision retention; P1-3 career total (通算成績) is on the profile page, not the result page — parsed there and used as source_url; P1-4 status mapping enumerated (取消→scratched, 除外→withdrawn nonstart, 中止→did_not_finish, 失格→disqualified); P1-5 basic_profile sourcing mapped to the real page (country from 産地 abbreviation, sex/color from title line, trainer suffix, pedigree from blood_table two generations six fields, romanized-only aliases); P1-6 fixture/test plan expanded (provider-bound adapter fixture, mismatch/suffix/missing-table/count-mismatch/overseas/status/year-only/障害 cases); P2 budget is 3 pages + 1 redirect so japan per-candidate budget raised 3→4 with no mid-candidate fallback; overseas detection rule specified; partial-expected-fields trap recorded; date-format blockers noted. Real fixtures captured from production: netkeiba_{horse,result,ped}_2022110137.html."
  - event: implemented
    date: "2026-07-22"
    session: 1
    mode: full
    artifacts_modified: [server/stable/services/p0_horse_completion_source_clients.py, server/stable/services/p0_horse_completion_batch.py, server/stable/services/p0_horse_completion_adapters.py, server/stable/test_p0_horse_netkeiba_client.py, server/stable/test_p0_horse_completion_batch.py, server/stable/test_p0_horse_completion_source_clients.py, server/stable/fixtures/p0_horse_completion/]
    note: "tasks 0.1-3.2 complete: _NetkeibaClient (3-page ID-direct fetch), _JapanDispatcherClient, select netkeiba preference, japan budget 3->4. End-to-end sqlite pipeline passes with auto first publish (profile complete_profile_full, four fields from page, verified key, published)."
  - event: code-reviewed
    date: "2026-07-22"
    session: 1
    mode: full
    rounds: 1
    issues_resolved: 8
    note: "Independent review round 1 resolved 2 P1 + 6 P2: P1-1 namespace iteration over frozenset source_names was nondeterministic across hash seeds for multi-key candidates (all regions) — reverted to deterministic identity_keys-order scan with netkeiba pre-pass for japan only; P1-2 title regex backtracking accepted '4歳' as color — color whitelist enforced, unknown shapes fail closed; P2 removed dead career_count_mismatch marker (adapter recomputes the gap), unknown single-char 産地 marks fail closed instead of silent 日本, NAR venue numeric prefixes (2大井8) handled, dispatcher batch_limit doc corrected (dispatcher enforces 1x), 4 missing scenario tests added (year-only birth, 障害 distance, adapter name-mismatch, suffixed-page pass), select determinism regression test added."
  - event: production-repair
    date: "2026-07-22"
    session: 1
    mode: full
    rounds: 1
    issues_resolved: 2
    note: "First production prepare (batch p0batch-4635b087fbbd) blocked 100/100 on two real defects: 61/100 netkeiba_profile_structure:title_sex_color — netkeiba serves EUC-JP without charset so requests decoded mojibake (fixtures were saved with explicit EUC-JP, hiding this); fixed with _netkeiba_page_text decoding raw bytes. 39/100 identity_incomplete — stale JBIS-era per-candidate cache served under netkeiba-keyed candidates, breaking provider-bound identity permanently; fixed with a japan-scoped cross-source cache guard in run_p0_horse_completion_adapter (US cross-provider complement flow unchanged). Regression tests: EUC-JP byte responses, cross-source cache miss, matching-source cache hit. Completion suites 270/270 green."
  - event: plan-reviewed
    date: "2026-07-23T02:15:28+08:00"
    session: 2
    mode: full
    rounds: 2
    issues_resolved: 6
    artifacts_modified: [proposal.md, design.md, specs/horse-profile-data-completion/spec.md, tasks.md]
    note: "Second production-discovery plan review resolved 1 P0 + 3 P1 + 2 P2: split deployment, network prepare, xlsx review, bundle/hash freeze, exact commit/publication authorization and final verification into separate gates; restore ALLOW_NETWORK=false in the prepare-window finally path; bind parser version to both batch fingerprints and netkeiba canonical cache; characterize the uncertain blank-finish career row without guessing; correct the three-page/career-total source and dispatcher batch-limit wording. Round 2 converged after the final network-window restoration fix."
  - event: apply-started
    date: "2026-07-23T02:15:41+08:00"
    tasks_total: 26
    branch: "claude/p0-horse-batch-completion"
  - event: code-reviewed
    date: "2026-07-23T02:49:10+08:00"
    session: 2
    mode: full
    rounds: 2
    issues_resolved: 1
    artifacts_modified: [server/stable/services/p0_horse_completion_adapters.py, server/stable/test_p0_horse_netkeiba_client.py, design.md, specs/horse-profile-data-completion/spec.md, tasks.md, docs/current_state.md, docs/decisions.md, docs/deploy_runbook.md, docs/project_status.md]
    note: "Independent native uncommitted review found one P1: a parser-version cache miss fetched a fresh payload but the existing stale file won no-clobber publication and was reread. Fixed with a netkeiba-only sidecar flock, in-lock version recheck and atomic os.replace; focused single-thread and concurrent tests prove all callers persist and reuse one current-version payload while ordinary cache, JBIS and other regions remain unchanged. Same reviewer continuous re-review cleared all actionable findings; final fingerprint is rechecked after this status write-back."
  - event: integration-review-required
    date: "2026-07-23T03:00:44+08:00"
    base: "0dcdbdabd6f3f4970eebf5284f29c488eb9340aa"
    review_scope: "base review from immutable base; final commit and content identity are recorded by the external review report to avoid self-referential stale SHAs"
    note: "The reviewed fix was rebased onto the latest origin/main before deployment. One deploy_runbook conflict retained all newer main release evidence and appended this change's section; code rebased without conflict. Focused tests are 285/285, OpenSpec 37/37, and full stable 2741 has the same 21 failures + 70 errors + 57 skipped as the clean origin/main 2726 baseline. The previous fingerprint and deployment authorization are invalid until the same reviewer approves this integrated version and the user authorizes that exact result."

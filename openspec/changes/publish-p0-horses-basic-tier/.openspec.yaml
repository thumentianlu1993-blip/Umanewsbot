schema: spec-driven
created: 2026-07-22
profile: feature
phase: reviewed
journal:
  - event: plan-reviewed
    date: "2026-07-22"
    session: 1
    mode: full
    rounds: 1
    issues_resolved: 8
    artifacts_modified: [proposal.md, design.md, tasks.md, specs/horse-profile-pages/spec.md]
    note: "Round 1 resolved 2 P0 + 3 P1 + 3 P2: P0-1 sync writes whitelisted-namespace keys by name attribution with no provenance — introduced source_refs.horse_identity_verified_keys written only by fail-closed enrichment commit or human-approved batch commit; gate reads only verified keys (stock pool corrected to 2,789 enrichment-verified minus 12 published; UK 6,342 sync keys excluded); P0-2 delta spec MODIFIED targets did not exist and the SHALL-NOT auto-publish requirement was untouched — delta restructured (MODIFIED 马匹资料审核控制公开可见性 + 首批公开验收必须由人工发布触发, gate/badge moved to ADDED, Purpose wording deferred to spec-sync); P1-1 hidden-resurrection quirk — gate also blocks hidden_at non-null; P1-2 create_new profiles leak — publish set includes completion-run reverse lookup; P1-3 partial publish failure must block committed terminal status via state.errors + raise; P2 region gate uses P0_REGIONS membership, auto_publish_blocked ops procedure documented, completion_run None handled."
  - event: implemented
    date: "2026-07-22"
    session: 1
    mode: full
    artifacts_modified: [server/stable/services/horse_profile_publish.py, server/stable/services/p0_horse_completion_commit.py, server/stable/services/p0_horse_production_apply.py, server/stable/services/p0_horse_identity_enrichment.py, server/stable/management/commands/publish_p0_horse_profiles.py, server/stable/management/commands/p0_horse_completion_batch.py, server/stable/models.py, server/stable/templates/stable/public/horse_index.html, server/stable/templates/stable/public/horse_detail.html, server/stable/test_horse_profile_publish.py, server/stable/test_p0_horse_completion_batch.py, server/stable/tests.py]
    note: "tasks 0.1-6.1 complete: BASIC gate on verified provenance, batch-commit auto first publish with retry-publish recovery stage (same-artifact full re-commit intentionally untouched — snapshot drift fails closed), stock publish command, public 资料补全中 badge. Target suites green; full stable 2569 tests identical to branch baseline (14F+70E, zero new)."
  - event: code-reviewed
    date: "2026-07-22"
    session: 1
    mode: full
    rounds: 1
    issues_resolved: 9
    note: "Independent review round 1 resolved 3 P1 + 6 P2: P1-1 retry_region_publish must require idempotent_verification.passed (commit stage is recorded before the verification raise); P1-2 committed terminal status now requires a completed publish stage for EVERY committed region (multi-region); P1-3 production provenance backfill step (re-run approved enrichment manifests, idempotent) added to runbook and tasks 7.3; P2 gate requires non-empty identifier in keys, save+OperationLog in one per-profile transaction, cumulative published ids merged across retries, publish-stage errors cleared on any successful publish, stock command exits non-zero on per-profile errors, service-level active-superuser check. Regression tests added for each."

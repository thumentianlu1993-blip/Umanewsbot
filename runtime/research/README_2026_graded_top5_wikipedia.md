# 2026 重赏前五名 Wikipedia 研究任务

这是一次只读研究任务，用于从 UmaFans 公开赛事页提取 2026 年指定地区已完赛重赏的前五名，并生成马匹三语名称与 Wikipedia 映射 artifact。

- 不写生产数据库。
- 不修改 RaceEvent、RaceEventResult、HorseProfile 或 TermEntry。
- 草稿 PR 仅用于触发并跟踪 GitHub Actions；不计划合并。
- 输出以 UmaFans 当前公开、data-quality-complete 的赛事页为覆盖基线，不声称独立证明外部目录全量覆盖。
